# TriEmoji
The semantic layer encodes each emoji's textual definition. The visual layer encodes its image. The LLM layer encodes the emoji as a token in a language model. Comparing these layers lets us see how meaning is captured differently in text, vision, and AI (LLM) contexts.
